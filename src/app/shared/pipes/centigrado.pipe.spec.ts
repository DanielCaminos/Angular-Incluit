import { CentigradoPipe } from './centigrado.pipe';

describe('CentigradoPipe', () => {
  it('create an instance', () => {
    const pipe = new CentigradoPipe();
    expect(pipe).toBeTruthy();
  });
});
